# .NET
bin/
obj/
*.user
.vs/

# EF Core scaffold - real credentials, never commit (see scaffold-config.template.json)
backend/**/scaffold-config.json

# Flutter/Dart (frontend, added in Phase 5)
frontend/**/.dart_tool/
frontend/**/build/
frontend/**/.flutter-plugins
frontend/**/.flutter-plugins-dependencies

# Secrets / local env
.env
*.pfx

# OS / editor
.DS_Store
Thumbs.db
